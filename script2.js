// let sum = 0;
// let subtraction = 0;
// let multiplication = 1
let division = 1
let firstNumber 
let secondNumber
let operator
let displayValue1 = 0
let displayValue2 = ""
let result

function add() {
    return result = Array.from(arguments).reduce((sum, current) => sum + current, 0)
}

function subtract() {
    return result = Array.from(arguments).reduce((subtraction, current) => subtraction - current, )
}

function multiply() {
    return result = Array.from(arguments).reduce((multiplication, current) => multiplication * current, )
}

function divide() {
    return result = Array.from(arguments).reduce((division, current) => division / current, )
}

multiply(6,2,1)
console.log(result)